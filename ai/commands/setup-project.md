Run `dotfiles link .` to copy shared config files into this project.

If the `dotfiles` command is not available, tell me how to add it to my PATH:
```sh
export PATH="$HOME/dev/i-dotfiles/bin:$PATH"
```

After linking, show what was copied with `dotfiles status .`.
