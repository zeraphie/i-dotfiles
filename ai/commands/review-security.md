Read the project's security rules at `.ai/SECURITY.md`.

Check the git remote to determine context:
- `Advisa` org → fintech (apply the fintech section too)
- Otherwise → standard rules only

Review the code I'm working on for security and privacy issues. For each finding, categorise it:

- 🔴 **CRITICAL** — security vulnerabilities, data leaks, broken auth (must fix before shipping)
- 🟡 **WARNING** — privacy risks, missing protections (address soon)
- 🔵 **INFO** — notes, future considerations (monitor)

If no issues found: `✅ No issues identified.`

If the security rules file is missing, say so and stop.
